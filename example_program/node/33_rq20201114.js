Express Statement
:DB 연결

Req Import
:req 는 Nodejs에서  지원하는 lib  / Import는 ES6가 된 문법 

즉시 실행 함수 
:

GET/ SET
:객체의 무결성을 보장이를 방지하기 위해, 필드를 private로 만들어 외부의 접근을 제한한 후,
Setter를 사용해 전달받은 값을 내부에서 가공해 필드에 넣어주는 방식을 사용하고
:마찬가지로 필드 값을 가져올 때도, Getter를 사용해 본 필드의 값을 숨긴 채내부에서 가공된 값을 꺼낼 수 있습니다.

포스트맨 설치
:했죠?